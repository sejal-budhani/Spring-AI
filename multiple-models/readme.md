Multiple AI models are required for
- Task specialization which a particular model can do very well
- Reliability if one model fails to respond, the other will do
- Experimentation
- User choice about which model to use
- Complementary strength to combine results from both models